{
  "buildCommand": "pnpm install --frozen-lockfile && pnpm --filter @workspace/barbershop-saas run build",
  "outputDirectory": "artifacts/barbershop-saas/dist/public",
  "rewrites": [
    {
      "source": "/(.*)",
      "destination": "/index.html"
    }
  ]
}
